# ============================================================
# 代码漏洞检测 / 漏洞分类（BERT 微调）依赖
# 安装： pip install -r requirements.txt
# ============================================================

# --- 深度学习框架 ---
# CPU 版（默认，仅用于跑通流程 / 无显卡环境）
torch>=2.2
# GPU 版请改用官方源安装，例如 CUDA 12.1：
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# CUDA 11.8：
#   pip install torch --index-url https://download.pytorch.org/whl/cu118

# --- 预训练模型 ---
transformers>=4.45
accelerate>=1.0
safetensors>=0.4
huggingface_hub>=0.34

# --- 数据处理 ---
datasets>=3.0
pandas>=2.0
pyarrow>=15.0
numpy>=1.26

# --- 评估与工具 ---
scikit-learn>=1.3
pyyaml>=6.0
requests>=2.31
tqdm>=4.66
