# 模型运行完整流程与测试用例

> 从环境自检到训练、推理、Web 部署的完整操作手册，附可直接运行的测试用例。
>
> **本文所有命令都已在本机实测通过**，输出为真实结果。
> 第 0 节的快速流程走 CPU 即可；正式训练用 conda `py312` 环境（RTX 3060 Laptop 6 GB，实测可用）。
>
> 配套文件：
> - 数据结构与原理详解 → [`./01_数据集与模型全解析.md`](./01_数据集与模型全解析.md)
> - 检测测试用例 → [`./test_cases/detection_test_cases.jsonl`](./test_cases/detection_test_cases.jsonl)
> - 分类测试用例 → [`./test_cases/classification_test_cases.jsonl`](./test_cases/classification_test_cases.jsonl)
> - 预处理单元测试 → [`./test_cases/test_preprocess.py`](./test_cases/test_preprocess.py)

---

## 0. 五分钟跑通（TL;DR）

如果你只想**确认整条流水线能跑**，按顺序执行下面 4 条命令即可（CPU 版，合计约 40 秒）：

```bat
cd /d D:\workbuddy_workspace\vuln_bert
set PYTHONPATH=%CD%

:: 有显卡用 conda py312（推荐）；只想验证流程也可以用隔离 venv
set PY=D:\Dev\Miniconda\envs\py312\python.exe
:: set PY=C:\Users\awu70\.workbuddy\binaries\python\envs\vuln_bert\Scripts\python.exe

%PY% scripts\check_env.py                                          :: ① 环境自检
%PY% docs\test_cases\test_preprocess.py                            :: ② 预处理 44 项单元测试
%PY% scripts\train.py --epochs 1 --max-train-samples 64 ^
      --max-eval-samples 32 --batch-size 8 --max-length 64 ^
      --run-name quickstart                                       :: ③ 冒烟训练（CPU 约 15 s，GPU 约 5 s）
%PY% scripts\predict.py --checkpoint outputs\quickstart\best ^
      --input docs\test_cases\detection_test_cases.jsonl ^
      --output docs\test_cases\out_detection.jsonl                 :: ④ 批量推理
```

> 想去掉 GPU 依赖时把最后两行的 `--cpu` 加回去即可（第 ③ 步的命令里已省掉）。

各步骤耗时与预期结果：

| 步骤 | 耗时 | 预期结果 |
|------|-----:|----------|
| ① 环境自检 | 5 s | 全部 `[ OK ]`；conda 环境会打印 `CUDA 可用 … RTX 3060 Laptop GPU 显存 6.0 GB` |
| ② 单元测试 | < 1 s | `通过 44 项，失败 0 项` + `全部通过` |
| ③ 冒烟训练 | CPU 15 s / GPU 5 s | `训练完成 ✅`，生成 `outputs/quickstart/best/` |
| ④ 批量推理 | 7 s | 生成 `docs/test_cases/out_detection.jsonl`，10 条预测 |

> ⚠️ **冒烟模型的预测结果全部是垃圾**（准确率 40%，接近瞎猜）。它的唯一作用是证明
> "数据能读、模型能建、能训练、能保存、能加载、能推理"。要看真实效果必须做第 6 步的正式训练。

---

## 1. 环境准备

### 1.1 本机已验证的环境（直接可用）

本机有两个可用的 Python 环境，**训练用 `py312`，只跑流程用 `vuln_bert` venv**：

| 项目 | conda `py312`（推荐，有 GPU） | 隔离 venv `vuln_bert`（CPU） |
|------|------------------------------|------------------------------|
| 解释器 | `D:\Dev\Miniconda\envs\py312\python.exe` | `C:\Users\awu70\.workbuddy\binaries\python\envs\vuln_bert\Scripts\python.exe` |
| Python | 3.12.13 | 3.13.14 |
| torch | 2.14.0+**cu130** | 2.14.0+**cpu** |
| transformers | 5.16.1 | 5.17.0 |
| numpy / sklearn | 2.5.2 / 1.9.0 | 2.5.3 / 1.9.1 |
| pandas / pyarrow | 3.0.5 / 25.0.1 | 3.0.5 / 25.0.1 |
| flask | 3.1.3 | 3.1.3 |
| **GPU** | **RTX 3060 Laptop，6.0 GB，sm_86，bf16 支持** | 无 |
| accelerate | 未安装（本项目不用，无影响） | 未安装 |

公共资源（两个环境共用）：

| 项目 | 值 |
|------|-----|
| 工作目录 | `D:\workbuddy_workspace\vuln_bert` |
| CodeBERT | `models/microsoft__codebert-base/`（477 MB，完整） |
| 数据 | `data/raw` 2.18 GB + `data/processed` 1.9 GB |

**实测训练速度**（CVEfixes 检测任务，不同 max_length）：

| 设备 | max_length | batch_size | 吞吐 | 单 epoch 估算（15,147 条） |
|------|-----------:|-----------:|-----:|---------------------------:|
| CPU（venv） | 64 | 8 | 5.7 samples/s | ~44 分钟 |
| **GPU（py312）** | 256 | 8 | **43.2 samples/s** | ~6 分钟 |
| **GPU（py312）** | 512 | 8 | **18.8 samples/s** | ~13 分钟 |

> 6 GB 显存实测可以跑 `batch_size=8 + max_length=512`，**不会 OOM**。
> 完整 3 轮训练预计 40 分钟出头。

```bat
:: 方式一：直接用绝对路径调用 conda 环境的 python（最简单，不用激活）
D:\Dev\Miniconda\envs\py312\python.exe scripts\check_env.py

:: 方式二：激活 conda 环境
call D:\Dev\Miniconda\Scripts\activate.bat py312
python scripts\check_env.py

:: 方式三（CPU，跑流程用）：激活隔离 venv
C:\Users\awu70\.workbuddy\binaries\python\envs\vuln_bert\Scripts\activate.bat
```

### 1.2 从零搭建（换机器时）

```bat
cd /d D:\workbuddy_workspace\vuln_bert
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python scripts\check_env.py
```

> `requirements.txt` **不含 flask**（工程版不需要 Web 部署）。
> 要跑教学版的网页功能，额外执行 `pip install flask`。

### 1.3 GPU 用户额外一步

> ✅ **本机的 conda `py312` 环境已经装好 CUDA 版 PyTorch（2.14.0+cu130），这一步可以跳过。**
> 下面只在你换机器 / 重建环境时才需要。

```bat
:: 先看驱动支持的 CUDA 版本
nvidia-smi
:: 再装对应版本的 PyTorch（cu121 → cu118 按需替换）
pip install torch --index-url https://download.pytorch.org/whl/cu121
```

装好后 `check_env.py` 会打印 GPU 型号与显存，`train.py` 会自动选 CUDA、自动开混合精度。

### 1.4 三条命令约定

1. **所有命令都在项目根目录执行**（`D:\workbuddy_workspace\vuln_bert`，教学版则是 `D:\workbuddy_workspace\vuln_bert_教学版`），
   否则 `configs/config.yaml` 这类相对路径会找不到。
2. **工程版需要 `PYTHONPATH` 指向项目根**（`set PYTHONPATH=%CD%`），因为 `scripts/*.py` 里是
   `from src.config import ...`；教学版不需要（它自己在脚本里 `sys.path.append`）。
3. **中文输出乱码时**先执行 `chcp 65001`，再 `set PYTHONIOENCODING=utf-8`。

---

## 2. 完整运行流程（9 步）

### 步骤 1 —— 环境自检

**目的**：确认依赖、GPU、数据、模型是否齐备，避免跑到一半才发现缺东西。

```bat
cd /d D:\workbuddy_workspace\vuln_bert
set PYTHONPATH=%CD%
python scripts\check_env.py
```

**实测输出**：

```
[ OK ] Python 3.13.14  (Windows AMD64)
--- Python 依赖 ---
[ OK ] torch            2.14.0+cpu
[ OK ] transformers     5.17.0
[ OK ] datasets         5.0.1        [ OK ] pandas      3.0.5
[ OK ] pyarrow          25.0.1       [ OK ] sklearn     1.9.1
[ OK ] numpy            2.5.3        [ OK ] yaml        6.0.3
[ OK ] requests         2.34.2       [ OK ] huggingface_hub 1.31.0
--- 硬件 ---
[WARN] 未检测到 CUDA，将使用 CPU 训练（非常慢，仅适合冒烟测试）
--- 原始数据 data/raw ---
[ OK ] cvefixes      3 个文件  1123.6 MB
     合计 1123.6 MB
--- 处理后数据 data/processed ---
[ OK ] 数据源 cvefixes  原始 18,925 条
       classification   类别数=41   train=5,832, val=729, test=730
--- 本地模型 models/ ---
[ OK ] microsoft__codebert-base  完整
```

**通过标准**：依赖部分无 `[FAIL]`；`data/raw` 合计 2180 MB；`models/` 显示"完整"。
`[WARN]` 只有 GPU 那一行是正常的（本机没显卡）。

### 步骤 2 —— 下载原始数据（已完成，可跳过）

```bat
python scripts\download_data.py --list                 :: 查看有哪些数据集
python scripts\download_data.py --datasets cvefixes    :: 只下 CVEfixes（1.2 GB）
python scripts\download_data.py --datasets all         :: 全下（2.1 GB）
python scripts\download_data.py --datasets all --mirror hf-mirror   :: 国内镜像
```

**通过标准**：`data/raw/cvefixes/` 下有 3 个 `train-*.parquet` 文件。
脚本支持断点续传（先下 `.part`，下完才改名），重复执行会跳过已完成的文件。

### 步骤 3 —— 下载预训练模型（已完成，可跳过）

```bat
python scripts\download_model.py --list
python scripts\download_model.py --models codebert
```

**通过标准**：`models/microsoft__codebert-base/` 下有 `config.json`、`pytorch_model.bin`（约 477 MB）、
`vocab.json`、`merges.txt`。

### 步骤 4 —— 构建统一数据集

**目的**：把 4 个格式不同的原始数据集清洗成统一 JSONL、按 `group_id` 划 8:1:1。

```bat
python scripts\build_dataset.py --source cvefixes                    :: 推荐：1.9 万检测 + 7,291 分类
python scripts\build_dataset.py --source cvefixes --task detection   :: 只构建检测数据
python scripts\build_dataset.py --source cvefixes --task classification  :: 只构建分类数据
```

**实测输出（cvefixes）**：

```
[INFO] 从 cvefixes 抽取原始记录 ...
[INFO] 抽取完成：18,925 条（过滤掉过短样本 N 条）
[INFO] 去重：19,xxx -> 18,925
[INFO] [detection/train] 15,147 条 -> cvefixes_detection_train.jsonl
[INFO] [detection/val]   1,876 条 -> cvefixes_detection_val.jsonl
[INFO] [detection/test]  1,902 条 -> cvefixes_detection_test.jsonl
[INFO] [classification/train] 5,832 条 ...
[INFO] [classification] 类别数 = 41 ...
[INFO] 全部完成 ✅
```

**通过标准**：

| 检查项 | 期望值 |
|--------|-------:|
| `cvefixes_detection_*` 合计 | 18,925 |
| `cvefixes_classification_*` 合计 | 7,291 |
| `cvefixes_classification_label_map.json` 的 `num_labels` | 41 |

跑 `python docs\test_cases\test_preprocess.py` 的第 [6][7] 组会自动核对这两项。

> 注意：`cvefixes_stats.json` 每次运行都会**覆盖**，只记录最后一次跑的任务。
> 想要完整统计就分别跑 `--task detection` 和 `--task classification`。

### 步骤 5 —— 冒烟训练（CPU，约 15 秒）

**目的**：用 64 条数据、1 轮、序列长度 64，验证"读取数据 → 建模型 → 训练 → 评估 → 保存 → 加载 → 测试"全链路。

```bat
python scripts\train.py --task detection --source cvefixes ^
    --model models\microsoft__codebert-base ^
    --epochs 1 --batch-size 8 --max-length 64 ^
    --max-train-samples 64 --max-eval-samples 32 ^
    --log-steps 4 --run-name quickstart --cpu
```

**实测输出**：

```
[15:09:05] INFO  任务=detection | 数据源=cvefixes | 模型=models\microsoft__codebert-base
[15:09:05] INFO  设备=cpu | 精度=no | 随机种子=42
[15:09:05] INFO  按配置截断样本：15,147 -> 64
[15:09:05] INFO  按配置截断样本：1,876 -> 32
[15:09:05] INFO  按配置截断样本：1,902 -> 32
[15:09:05] INFO  样本数  train=64  val=32  test=32  类别数=2
[15:09:05] INFO  模型参数量: 总计 124.6M，可训练 124.6M
[15:09:05] INFO  类别权重: [0.762, 1.455]
[15:09:05] INFO  启用加权交叉熵损失
[15:09:05] INFO  优化步数: 每 epoch 8 步，共 8 步（梯度累积 1）
[15:09:11] INFO  epoch 1 | step 4/8 | loss 0.6838 | lr 1.00e-05 | 5.6 samples/s
[15:09:17] INFO  epoch 1 | step 8/8 | loss 0.7549 | lr 0.00e+00 | 5.7 samples/s
[15:09:18] INFO  epoch 1 完成 | train_loss=0.7549 | val: accuracy=0.6562 | precision=0.0000 |
                 recall=0.0000 | f1=0.0000 | roc_auc=0.4632 | tn=21 | fp=0 | fn=11 | tp=0
[15:09:18] INFO  ✅ 新的最佳模型已保存（f1=0.0000）-> ...\outputs\quickstart\best
[15:09:19] INFO  [VAL]  accuracy=0.6562 | f1=0.0000 | roc_auc=0.4632
[15:09:20] INFO  [TEST] accuracy=0.4688 | f1=0.0000 | roc_auc=0.4118
[15:09:20] INFO  训练完成 ✅ 结果已保存 -> ...\results.json
```

**通过标准**：

- 日志里能看到 `模型参数量: 总计 124.6M`（说明 CodeBERT 加载成功）
- `每 epoch 8 步`（说明数据管道正常）
- `✅ 新的最佳模型已保存`
- `outputs/quickstart/` 下出现 11 个产物文件（见下方清单）
- **F1=0.0000 是正常的**，64 条数据训练不出任何东西

**产物清单**（`outputs/quickstart/`）：

```
config.yaml            生效配置（可复现）
results.json           每轮指标 + 测试集指标
best/pytorch_model.bin 验证集最优权重（约 500 MB）
best/tokenizer.json    分词器
best/tokenizer_config.json
best/label_map.json    任务类型 + 类别数 + id2name
logits_val.npy / labels_val.npy / probs_val.npy
logits_test.npy / labels_test.npy / probs_test.npy
```

### 步骤 6 —— 正式训练（需要 GPU）

```bat
:: 任务 A：漏洞检测（二分类）
python scripts\train.py --task detection --source cvefixes --model models\microsoft__codebert-base

:: 任务 B：漏洞分类（CWE 多分类，41 类）
python scripts\train.py --task classification --source cvefixes --model models\microsoft__codebert-base
```

> ⚠️ **务必用本地路径 `models\microsoft__codebert-base`，不要写 `microsoft/codebert-base`。**
> 后者是 HuggingFace 仓库名，会触发一次约 1 GB 的重复下载
> （`pytorch_model.bin` 473 MB + `model.safetensors` 499 MB），而你本地已经有同一份权重。
> 而且这个名字会被记进 `outputs/<run>/config.yaml`，以后 `predict.py` 也会去读线上缓存。

默认配置（`configs/config.yaml`）：`epochs=3`、`batch_size=16`、`lr=2e-5`、`head_lr=1e-4`、
`max_length=512`、`warmup_ratio=0.1`、`early_stopping_patience=2`。

**耗时参考**（本机实测 + 估算）：

| 设备 | 配置 | 吞吐 | CVEfixes 检测 1 epoch |
|------|------|-----:|----------------------|
| **RTX 3060 Laptop 6 GB** | `batch 8 / max_length 512` | 18.8 samples/s | **约 13 分钟** |
| **RTX 3060 Laptop 6 GB** | `batch 8 / max_length 256` | 43.2 samples/s | 约 6 分钟 |
| 隔离 venv（CPU） | `batch 8 / max_length 64` | 5.7 samples/s | 约 44 分钟 |
| RTX 3090（README 值） | 默认 | — | 约 15 分钟 |

> 建议直接用默认配置（`batch_size=16` 先试，OOM 再降到 8）。
> 本机 6 GB 显存实测 `batch_size=8 + max_length=512` **不会 OOM**。

> 📌 **想训得更久（接着 3 轮继续）看 [`03_项目交接与迁移指南.md`](./03_项目交接与迁移指南.md) 第 4.1、4.2 节。**
> 关键结论：训练好的 `best/` **不能**当基座模型继续训练（里面没有 `config.json`，实测报
> `ValueError: Unrecognized model`），只能重新跑 `--epochs 6`；
> 而且预热步数会随总步数一起变（284 → 568），所以 6 轮不是 3 轮的延长线。

**显存不够时**：

```bat
:: 方案一：降 batch + 梯度累积（等效 batch 仍是 16）
python scripts\train.py --batch-size 4 --max-length 256
:: 然后到 configs/config.yaml 里把 gradient_accumulation_steps 改成 4

:: 方案二：直接降序列长度
python scripts\train.py --batch-size 8 --max-length 256
```

**常用覆盖参数**：

| 参数 | 说明 |
|------|------|
| `--model` | 预训练模型名或本地路径 |
| `--epochs` / `--batch-size` / `--lr` | 轮数 / 批大小 / 学习率 |
| `--max-length` | 512 / 256 / 128 |
| `--max-train-samples` | 限制训练样本数（调试） |
| `--imbalance` | `weighted_loss`（默认）/ `balanced_sampler` / `none` |
| `--run-name` | 输出子目录名 |
| `--cpu` | 强制 CPU |

**通过标准**：`outputs/<run_name>/results.json` 里 test 的 F1（检测）或 macro_f1（分类）达到第 4.7 节的预期区间。
分类任务还会额外生成 `report_test.txt`（逐类别 P/R/F1）。

### 步骤 7 —— 推理

**方式一：单段代码**

```bat
python scripts\predict.py --checkpoint outputs\quickstart\best ^
    --code "void f(char *s){ char b[10]; strcpy(b,s); }" --device cpu
```

⚠️ **含 `{}` 或引号的复杂代码不要用 `--code`**——cmd 会把参数拆坏，报
`unrecognized arguments`。用下面的方式二。

**方式二：从文件读取（推荐）**

```bat
:: 先把代码存成 demo.c，再：
python scripts\predict.py --checkpoint outputs\quickstart\best --file demo.c --device cpu
```

**实测输出**：

```
[15:05:08] INFO  模型加载完成 | 任务=detection | 类别数=2 | 设备=cpu

============================================================
判定结果 : safe
置信度   : 0.5551
安全概率 : 0.5551
漏洞概率 : 0.4449
============================================================
```

**方式三：批量（JSONL）**

```bat
python scripts\predict.py --checkpoint outputs\quickstart\best ^
    --input docs\test_cases\detection_test_cases.jsonl ^
    --output docs\test_cases\out_detection.jsonl --device cpu --batch-size 4
```

**实测输出**：

```
[15:09:43] INFO  模型加载完成 | 任务=detection | 类别数=2 | 设备=cpu
[15:09:43] INFO  批量推理 10 条
  已处理 10/10
[15:09:43] INFO  与 gold 标签对比准确率: 0.4000 (4/10)
[15:09:43] INFO  预测结果 -> docs\test_cases\out_detection.jsonl
```

**分类任务的输出格式**：

```
预测 CWE : CWE-78  (置信度 0.6120)
Top-5    :
   CWE-78       0.6120
   CWE-89       0.1903
   ...
```

> ⚠️ **分类批量推理的坑**：输入的 JSONL 里如果放字符串 `label`（如 `"CWE-120"`），
> 脚本会尝试 `int("CWE-120")` 而崩溃：`ValueError: invalid literal for int() with base 10`。
> 所以分类测试用例用的是 `expect_cwe` 字段而不是 `label`。

### 步骤 8 —— Web 部署（仅教学版）

```bat
cd /d D:\workbuddy_workspace\vuln_bert_教学版\_02_bert检测
python _07_基于flask的web部署.py
:: 浏览器打开 http://127.0.0.1:9091，粘贴代码 → 点"开始检测"
```

分类任务的 Web 版在 `_03_bert分类\_07_基于flask的web部署.py`，端口 **9092**。

**前提**：`_02_bert检测/model/bert_detect_model.pt` 存在（本机已有，475.6 MB）。
分类模块的 `model/` 目录是**空的**，必须先跑 `_03_bert分类\_05_bert模型训练代码_gpu.py`。

**不用启动服务器也能验证**（用 Flask 测试客户端，实测通过）：

```python
from flask import Flask
import importlib.util, sys
spec = importlib.util.spec_from_file_location("webapp", "_07_基于flask的web部署.py")
mod = importlib.util.module_from_spec(spec); spec.loader.exec_module(mod)
client = mod.app.test_client()
print(client.get("/").status_code)                                   # 200
print(client.post("/", data={"text": "char b[10]; strcpy(b,s);"}).status_code)   # 200
```

### 步骤 9 —— 对比实验

```bat
python scripts\train.py --model microsoft/graphcodebert-base --run-name gcb_detection
python scripts\train.py --imbalance balanced_sampler --run-name bs_detection
python scripts\train.py --model bert-base-uncased    --run-name bert_base_detection
```

所有 `outputs/*/results.json` 可以直接横向比较。

---

## 3. 全流程数据流图

```
 ┌─────────────────── 原始数据 data/raw/ 1.1 GB ────────────────────┐
 │ cvefixes/*.parquet   13,000 条 CVE（25 字段）                     │
 └────────────────────────────┬─────────────────────────────────────┘
                              │ ① 字段归一化：映射成 id/code/label/cwe/group_id
                              │ ② _clean_code：统一换行符 + strip
                              │ ③ _norm_cwe：CWE-079 / 79 / cwe_79 → CWE-79
                              │ ④ 长度过滤：< 30 字符丢弃
                              │ ⑤ 头尾截断：> 20,000 字符 → 头 60% + 尾 40%
                              │ ⑥ MD5 去重
                              │ ⑦ 按 group_id 分 8:1:1（防泄漏）
                              │ ⑧ 分类任务：CWE → 0~40 编号，长尾合并 OTHER
                              ▼
 ┌───────────── data/processed/ 1.9 GB（统一 JSONL）─────────────┐
 │ cvefixes_detection_{train,val,test}.jsonl        18,925 条     │
 │ cvefixes_classification_{train,val,test}.jsonl    7,291 条     │
 │ *_label_map.json（41 类映射） *_stats.json                      │
 └────────────────────────────┬───────────────────────────────────┘
                              │ ⑨ truncate_code(code, 8000)
                              │ ⑩ tokenizer(max_length=512, truncation=True)
                              │ ⑪ 动态 padding（补到本 batch 最长，对齐 8）
                              ▼
                    input_ids / attention_mask / labels
                              │
                              ▼
 ┌──────────────── VulnClassifier ────────────────┐
 │ CodeBERT(124M，全参数微调) → [CLS]池化 → Dropout(0.1) → Linear(768,K) │
 └────────────────────────┬───────────────────────┘
                          │ ⑫ CrossEntropyLoss（weighted_loss）
                          │ ⑬ AdamW 分层学习率（2e-5 / 1e-4）+ warmup 10% + 线性衰减
                          │ ⑭ 梯度裁剪 1.0 / 混合精度 / 梯度累积
                          │ ⑮ 按 val F1(或 macro-F1) 选最优 + 早停 patience=2
                          ▼
 ┌────────── outputs/<run_name>/ ──────────┐
 │ config.yaml  results.json               │
 │ best/pytorch_model.bin  (约 500 MB)     │
 │ best/tokenizer.json  best/label_map.json│
 │ logits_*.npy  labels_*.npy  probs_*.npy │
 │ report_test.txt（分类任务）              │
 └────────────────────┬────────────────────┘
                      │ ⑯ 加载 best/ → 前向 → softmax → argmax
                      ▼
   检测：safe / vulnerable + 置信度 + 两个概率
   分类：CWE-xxx + 置信度 + Top-5 候选
                      │
                      ▼
         CLI（predict.py）/ Flask 网页（教学版 9091/9092）
```

---

## 4. 测试用例

### 4.1 设计原则

| 层次 | 测什么 | 文件 | 是否需要 GPU |
|------|--------|------|--------------|
| **单元级** | 清洗 / 截断 / CWE 规范化 / 分组划分 / 标签映射 | `test_cases/test_preprocess.py` | 否 |
| **端到端·检测** | 一段源码 → `safe` / `vulnerable` | `test_cases/detection_test_cases.jsonl` | 否 |
| **端到端·分类** | 一段漏洞代码 → CWE 类型 | `test_cases/classification_test_cases.jsonl` | 否 |

用例的选取遵循三条：

1. **覆盖高频 CWE**。CVEfixes 里样本最多的几类是 CWE-79（XSS）、CWE-125（越界读）、
   CWE-89（SQL 注入）、CWE-20（输入校验）、CWE-787/CWE-120（越界写）——用例优先覆盖它们。
2. **正负成对**。每个漏洞用例都配一条"修好之后"的安全用例（如 `det01` 溢出 vs `det07` 加了长度限制），
   这是最难的判别场景，也是这套数据的设计初衷。
3. **跨语言**。覆盖 C、PHP、Python，检验模型不是只记住了 C 的语法。

### 4.2 预处理单元测试（44 项断言，秒级完成）

```bat
cd /d D:\workbuddy_workspace\vuln_bert
set PYTHONPATH=%CD%
python docs\test_cases\test_preprocess.py
```

**实测输出**：

```
[1] _clean_code —— 统一换行符 + 去首尾空白
  [PASS] Windows 换行 \r\n → \n        [PASS] 老 Mac 换行 \r → \n
  [PASS] 去掉首尾空白                   [PASS] 非字符串返回空串
  [PASS] 数字也返回空串                 [PASS] 中间的空行保留
  [PASS] 注释不删                       [PASS] 缩进不压缩

[2] _norm_cwe —— 各种写法统一成 CWE-<数字>
  [PASS] 标准写法   [PASS] 纯数字   [PASS] 下划线小写   [PASS] 去前导零
  [PASS] None → 空  [PASS] nan → 空 [PASS] NVD-CWE-noinfo → 空
  [PASS] NVD-CWE-other → 空         [PASS] 纯文字 → 空  [PASS] 空格包裹

[3] truncate_code —— 头 60% + 尾 40%
  [PASS] 未超长时原样返回            [PASS] 恰好等于上限时原样返回
  [PASS] 超长时保留头 6 + 尾 4       [PASS] 截断后长度 = 上限 + 占位注释
  [PASS] 空字符串安全                [PASS] 占位注释说明从哪里断的

[4] _md5 —— 跨平台一致，可跨进程去重
  [PASS] 换行符统一后指纹相同  [PASS] 不同内容指纹不同  [PASS] 指纹是 32 位十六进制

[5] split_groups —— 同一 group_id 必须落在同一个 split
  [PASS] 划分比例约 8:1:1（按组）    [PASS] 训练/验证集无重叠组
  [PASS] 训练/测试集无重叠组         [PASS] 验证/测试集无重叠组
  [PASS] 所有组都被用到              [PASS] 每对 vuln/fixed 都没有被拆开
  [PASS] 同种子结果可复现

[6] 标签映射文件自检
  [PASS] 分类类别数 = 41   [PASS] 编号 0 是 CWE-79   [PASS] 编号 40 是 OTHER
  [PASS] id2name 与 name2id 互为逆映射             [PASS] id2name 覆盖全部 41 类
  [PASS] 检测类别数 = 2    [PASS] 0 = safe           [PASS] 1 = vulnerable

[7] 处理后数据文件自检
  [PASS] cvefixes detection 合计 18925 条
  [PASS] cvefixes classification 合计 7291 条

============================================================
通过 44 项，失败 0 项
============================================================
全部通过
```

**通过标准**：`失败 0 项`。任何一项 FAIL 都说明数据构建或工具函数被改坏了。

**断言明细**（挑几个关键的）：

| 断言 | 期望 | 为什么重要 |
|------|------|-----------|
| `_clean_code("a\r\nb") == "a\nb"` | 换行统一 | 不统一则 MD5 去重跨平台失效 |
| `_clean_code("// 注释\nx=1")` 保留注释 | 只 strip 不动内容 | 证明"清洗"没做过度处理 |
| `_norm_cwe("CWE-079") == "CWE-79"` | 去前导零 | 不去零会把同一 CWE 拆成两个类 |
| `_norm_cwe("NVD-CWE-noinfo") == ""` | 无效值置空 | 否则会多出一个假类别 |
| `truncate_code("a"*100, 10, 0.6)` | `aaaaaa…（注释）…aaaa` | 头尾比例必须精确（6:4） |
| `split_groups(...)` 三集合组无重叠 | 交集为空 | **防数据泄漏的核心断言** |
| `{2} == 每组的样本数` | 每对 vuln/fixed 未被拆开 | 拆开 = 测试集出现训练集的近邻副本 |
| 同 seed 两次划分结果相同 | 可复现 | 保证实验对比有意义 |

### 4.3 检测任务测试用例（10 条）

文件：[`./test_cases/detection_test_cases.jsonl`](./test_cases/detection_test_cases.jsonl)，
每条含 `id` / `code` / `label`（0=安全 1=漏洞）/ `cwe` / `note`。

| # | id | 语言 | 漏洞类型 | 代码要点 | 期望 label |
|--:|----|------|----------|----------|-----------:|
| 1 | `det01_cwe120_stack_overflow` | C | CWE-120 栈溢出 | `char buf[10]; strcpy(buf, user_input);` 不限长度 | **1** |
| 2 | `det02_cwe89_sql_injection` | PHP | CWE-89 SQL 注入 | `"SELECT … WHERE id = " . $_GET['id']` 字符串拼接 | **1** |
| 3 | `det03_cwe79_xss` | PHP | CWE-79 XSS | `echo "<div>" . $_GET['name'] . "</div>"` 未转义 | **1** |
| 4 | `det04_cwe78_cmd_injection` | C | CWE-78 命令注入 | `sprintf(cmd, "ls -l %s", argv[1]); system(cmd);` | **1** |
| 5 | `det05_cwe22_path_traversal` | Python | CWE-22 路径遍历 | `open(os.path.join('/var/www/reports', filename))` | **1** |
| 6 | `det06_cwe476_null_deref` | C | CWE-476 空指针 | `find_user()` 可能返回 NULL，直接 `u->name` | **1** |
| 7 | `det07_safe_strncpy` | C | — | `strncpy(buf, user_input, sizeof(buf)-1)` + 手动补 `\0` | **0** |
| 8 | `det08_safe_prepared_statement` | PHP | — | `$pdo->prepare('… WHERE id = ?')` 参数化查询 | **0** |
| 9 | `det09_safe_htmlspecialchars` | PHP | — | `htmlspecialchars($_GET['name'], ENT_QUOTES, 'UTF-8')` | **0** |
| 10 | `det10_safe_bounds_check` | C | — | `if (n >= dst_len) return -1;` 显式边界检查 | **0** |

**用心设计的几对**：

- `det01` ↔ `det07`：只差"有没有限制长度"和"有没有补终止符"，是模型最难分辨的一对
- `det02` ↔ `det08`：字符串拼接 vs 参数化查询，SQL 注入的经典正负对
- `det03` ↔ `det09`：直接输出 vs HTML 转义，XSS 的经典正负对

### 4.4 分类任务测试用例（7 条）

文件：[`./test_cases/classification_test_cases.jsonl`](./test_cases/classification_test_cases.jsonl)，
用 `expect_cwe` 存放期望类别（**不是 `label`**，原因见 4.6 的坑）。

| # | id | 语言 | 期望 CWE | 说明 |
|--:|----|------|----------|------|
| 1 | `cls01_cwe120` | C | **CWE-120** | 缓冲区溢出 |
| 2 | `cls02_cwe89` | PHP | **CWE-89** | SQL 注入 |
| 3 | `cls03_cwe79` | PHP | **CWE-79** | 跨站脚本 |
| 4 | `cls04_cwe78` | C | **CWE-78** | OS 命令注入 |
| 5 | `cls05_cwe22` | Python | **CWE-22** | 路径遍历 |
| 6 | `cls06_cwe416` | C | **CWE-416** | 释放后使用（use-after-free） |
| 7 | `cls07_cwe476` | C | **CWE-476** | 空指针解引用 |

> 期望值都是 CVEfixes 里样本量靠前的类别（CWE-79/89/22/78 在 Top-13 内），
> 理论上比长尾类别更容易学对。

### 4.5 怎么跑这些用例

**检测（可与 gold 自动比对，脚本会打印准确率）**：

```bat
python scripts\predict.py --checkpoint outputs\quickstart\best ^
    --input docs\test_cases\detection_test_cases.jsonl ^
    --output docs\test_cases\out_detection.jsonl --device cpu --batch-size 4
```

**分类（`expect_cwe` 不是 `label`，脚本不会自动比对，需自己看输出）**：

```bat
python scripts\predict.py --checkpoint outputs\<你的分类运行名>\best ^
    --input docs\test_cases\classification_test_cases.jsonl ^
    --output docs\test_cases\out_classification.jsonl --device cpu
```

**教学版（改脚本里的 `demo_code`）**：

```bat
cd /d D:\workbuddy_workspace\vuln_bert_教学版\_02_bert检测
python _06_bert_predict_fun.py
```

### 4.6 实测结果

#### A. 正式训练后的模型（`outputs/cvefixes_detection_codebert-base/best`，测试集 F1=0.7521）

```
python scripts\predict.py --checkpoint outputs\cvefixes_detection_codebert-base\best ^
    --input docs\test_cases\detection_test_cases.jsonl ^
    --output docs\test_cases\out_detection_real.jsonl --batch-size 4

→ 与 gold 标签对比准确率: 0.6000 (6/10)
```

逐条明细：

| 用例 | 期望 | 预测 | P(漏洞) | 结果 |
|------|-----:|------|--------:|:----:|
| det03 XSS | 1 | vulnerable | 0.9572 | ✅ |
| det05 路径遍历 | 1 | vulnerable | 0.8127 | ✅ |
| det02 SQL 注入 | 1 | vulnerable | 0.8011 | ✅ |
| det06 空指针解引用 | 1 | safe | 0.4712 | ❌ |
| det01 栈溢出 | 1 | safe | 0.3928 | ❌ |
| det04 命令注入 | 1 | safe | 0.1333 | ❌ |
| det08 参数化查询 | 0 | vulnerable | 0.7668 | ❌（误报） |
| det07 安全的 strncpy | 0 | safe | 0.2920 | ✅ |
| det09 HTML 转义 | 0 | safe | 0.0725 | ✅ |
| det10 边界检查 | 0 | safe | 0.0104 | ✅ |

**怎么解读这 6/10**：

- 这 10 条是 **2~3 行的短片段**，而训练数据是**完整函数**，属于分布外样本。
  它只能用来横向比较不同模型，**不能当成绝对水平**——真正的水平看测试集 F1=0.7521。
- 三个漏报（det01/04/06）的 P 值分别是 0.39/0.13/0.47，**det01 和 det06 只差一点**
  就过线了。把阈值从默认 0.5 降到 0.3，det01 和 det06 就能被抓出来 → 8/10。
- det01（有漏洞的 `strcpy`）和 det07（安全的 `strncpy`）这对**最难的正负对**，
  模型给出的分数是 **0.3928 vs 0.2920**——**排序是对的**，只是两个都低于 0.5 阈值。
  这正说明该调阈值（见 `01` 文档 8.2 节的阈值表）。

#### B. 冒烟模型（`outputs/quickstart/best`，64 条数据 1 轮 —— 全错，这是预期的）

用 `outputs/quickstart/best`（64 条数据、1 轮）跑检测用例：

```
与 gold 标签对比准确率: 0.4000 (4/10)
```

**4/10 是怎么来的**：模型把所有 10 条都判成 `safe`。
6 条真漏洞（det01~det06）全判错，4 条安全样本（det07~det10）全判对 → 4/10。
换句话说，它的准确率完全来自"多数类是安全样本"这个先验，**没有任何检测能力**。

用 `outputs/quickstart_cls/best`（41 类分类）跑分类用例：

| # | 期望 | 实际预测 | Top-1 置信度 | 对/错 |
|--:|------|----------|-------------:|:-----:|
| 1 | CWE-120 | CWE-77 | 0.0497 | ✗ |
| 2 | CWE-89 | CWE-77 | 0.0469 | ✗ |
| 3 | CWE-79 | CWE-77 | 0.0464 | ✗ |
| 4 | CWE-78 | CWE-399 | 0.0484 | ✗ |
| 5 | CWE-22 | CWE-77 | 0.0466 | ✗ |
| 6 | CWE-416 | CWE-77 | 0.0495 | ✗ |
| 7 | CWE-476 | CWE-89 | 0.0510 | ✗ |

**0/7**。所有置信度都在 **0.05 左右**（随机基线 1/41 ≈ 0.024），
说明模型在 41 个类别上是近似均匀乱猜——这正是没训练过的模型该有的表现。

> ✅ **这组"全错"恰恰是测试用例有效的证据**：如果没训练的模型也能答对，
> 说明用例要么太简单、要么数据泄漏了。

### 4.7 正式训练后的验收标准

做完步骤 6 的正式训练后，重新跑 4.5 的用例，按下表验收：

| 指标 | 合格线 | 说明 |
|------|-------:|------|
| 检测·CVEfixes 测试集 F1 | **≥ 0.75** | 本仓库 3 轮实测 0.7521；加到 5~6 轮有望 0.78~0.82。<br>根 README 里的"0.85+"是未经训练的估算值，不要当验收线 |
| （参考）BigVul/Devign 测试集 F1 | **0.55 ~ 0.65** | 文献量级，真实分布、任务更难；本项目不再使用这些数据源 |
| 检测·本用例集准确率 | **≥ 0.6** | 实测 6/10。注意这 10 条是 2~3 行的**短片段**，与训练数据的"完整函数"分布不同，只能横向比较不同模型，不能当绝对水平 |
| 分类·macro-F1 | 看 `report_test.txt` | 长尾类别会明显拉低，同时看 `weighted_f1` |
| 分类·本用例集 Top-1 | **≥ 5/7** | 这 7 类都是高频 CWE |
| 分类·Top-5 命中率 | **≥ 7/7** | 正确类别应该出现在 Top-5 里 |

**如果检测 F1 到了 0.99**，别高兴，先检查是不是划分方式错了
（按样本随机分而不是按 `group_id` 分）——见 `test_preprocess.py` 第 [5] 组断言。

**如果 F1 停在 0.75 左右**：先看 `results.json` 里 val F1 是否还在涨。
本项目实测就是这么个情况——3 轮时 val F1 还在一路上升，属于"训练不足"，
把 `--epochs` 加到 5~6 即可。另外记得调判定阈值（见 `01` 文档 8.2 节的阈值表）。

**如果 F1 只有 0.6 左右**：先确认有没有误用 `--max-train-samples` 导致训练不足。

---

## 5. 一键脚本

把下面的内容存成 `check_all.bat` 放在项目根目录，双击即可依次跑完所有检查：

```bat
@echo off
chcp 65001 >nul
set PYTHONIOENCODING=utf-8
cd /d %~dp0
set PYTHONPATH=%CD%
set PY=C:\Users\awu70\.workbuddy\binaries\python\envs\vuln_bert\Scripts\python.exe

echo ========== 1/5 Environment check ==========
%PY% scripts\check_env.py || goto :fail

echo ========== 2/5 Preprocess unit tests ==========
%PY% docs\test_cases\test_preprocess.py || goto :fail

echo ========== 3/5 Smoke training (CPU, ~15s) ==========
%PY% scripts\train.py --task detection --source cvefixes ^
    --model models\microsoft__codebert-base ^
    --epochs 1 --batch-size 8 --max-length 64 ^
    --max-train-samples 64 --max-eval-samples 32 ^
    --log-steps 4 --run-name check_all --cpu || goto :fail

echo ========== 4/5 Batch inference ==========
%PY% scripts\predict.py --checkpoint outputs\check_all\best ^
    --input docs\test_cases\detection_test_cases.jsonl ^
    --output docs\test_cases\out_detection.jsonl --device cpu || goto :fail

echo ========== 5/5 Done ==========
echo All checks passed. See docs\test_cases\out_detection.jsonl
pause
exit /b 0

:fail
echo.
echo [FAILED] See the message above.
pause
exit /b 1
```

---

## 6. 排错手册

| 现象 | 原因 | 处理 |
|------|------|------|
| `ModuleNotFoundError: No module named 'src'` | 没设 `PYTHONPATH`，或不在项目根目录执行 | `set PYTHONPATH=%D` 后重新执行 |
| **`[FAIL] datasets 未安装`（但确实装了）** | `src/` 被加进 sys.path，`import datasets` 加载到项目内的同名文件 | **已修复**：`src/datasets.py` 已改名 `src/data.py`。若你从旧版本升级，手动改名即可；`check_env.py` 现在会直接指出顶替者 |
| `ImportError: attempted relative import with no known parent package` | 同上，项目内的同名文件被当成顶层模块导入 | 同上 |
| `ValueError: Unrecognized model in ...best. Should have a model_type key in its config.json` | 把训练好的检查点当基座模型传给了 `--model`，但 `best/` 里没有 `config.json` | 训练要用 `--model models\microsoft__codebert-base` 重新跑；只有推理才用 `--checkpoint .../best`。详见 `03` 文档 4.1 |
| `缺少数据文件 …\cvefixes_detection_train.jsonl` | 还没构建数据集 | 先跑 `python scripts\build_dataset.py --source cvefixes` |
| `unrecognized arguments: copy_input(char *user_input)…` | `--code` 里的 `{}` / 引号被 cmd 拆坏 | 改用 `--file demo.c` |
| `ValueError: invalid literal for int() with base 10: 'CWE-120'` | 分类批量推理的 JSONL 里放了字符串 `label` | 去掉 `label` 或改成数字编号（见 4.6） |
| `CUDA out of memory` | batch 太大 | `--batch-size 4 --max-length 256` + `gradient_accumulation_steps: 4` |
| `CrossEntropyLoss: weight tensor should be defined either for all N classes` | 分类类别数漏了 `+1`（OTHER 类） | 类别数 = 保留的 CWE 数 + 1 |
| 中文日志乱码 | 控制台代码页不对 | `chcp 65001` + `set PYTHONIOENCODING=utf-8` |
| 训练进度条里 `it/s` 只有个位数 | 在 CPU 上跑 512 token | 正常。用 `--max-train-samples 200` 先跑通流程 |
| **训练时在下载 473MB + 499MB** | `--model microsoft/codebert-base` 写的是 HF 仓库名，会去线上下载；而本地 `models/microsoft__codebert-base` 已经有同一份权重 | 改成 `--model models/microsoft__codebert-base`。已经下过的不影响使用，只是白下了约 1 GB |
| `huggingface_hub` 提示 symlinks 不支持 | Windows 未开开发者模式 | 无害，只是缓存会多占点空间。可设 `set HF_HUB_DISABLE_SYMLINKS_WARNING=1` 关掉提示 |
| Windows 上 DataLoader 多进程报错 | `num_workers > 0` | 保持 `configs/config.yaml` 里的 `num_workers: 0` |
| `transformers` 报 `AutoModel` 相关错误 | 版本太旧 | `pip install -U "transformers>=4.45"` |
| Flask 启动报 `ModuleNotFoundError: flask` | 依赖里没有 flask | `pip install flask` |
| 下载超时 / 连接被重置 | 访问 huggingface.co 慢 | `set HF_ENDPOINT=https://hf-mirror.com`，或用 `--mirror hf-mirror` |

---

## 7. 验收清单

按顺序打勾，全部通过说明项目状态健康：

- [ ] `check_env.py` 无 `[FAIL]`，`data/raw` 合计 2180 MB，`models/` 显示"完整"
- [ ] `test_preprocess.py` 输出 `通过 44 项，失败 0 项` / `全部通过`
- [ ] `cvefixes_detection_*` 合计 18,925 条，`cvefixes_classification_*` 合计 7,291 条
- [ ] `cvefixes_classification_label_map.json` 的 `num_labels == 41`，编号 40 是 `OTHER`
- [ ] 冒烟训练能跑完并打印 `训练完成 ✅`，`outputs/<run>/best/` 有 4 个文件
- [ ] `results.json` 里能同时看到 `history` / `val` / `test` 三段
- [ ] 单条推理能输出"判定结果 / 置信度 / 安全概率 / 漏洞概率"四项
- [ ] 批量推理能输出 `accuracy`（输入带 `label` 时）
- [ ] 教学版 `_06_bert_predict_fun.py` 能加载 `.pt` 并打印"预测结果"
- [ ] 教学版 Flask 页面 `GET /` 和 `POST /` 都返回 200
- [ ] （正式训练后）检测 F1 ≥ 0.85，10 条检测用例准确率 ≥ 0.8
- [ ] （正式训练后）分类用例 Top-1 ≥ 5/7，Top-5 = 7/7

---

## 附录：本次验证实际执行过的命令

```bat
:: ① 环境
python scripts\check_env.py

:: ② 分词器演示（教学版）
cd D:\workbuddy_workspace\vuln_bert_教学版\00_test
python _03_测试分词器.py

:: ③ 预处理单元测试
python docs\test_cases\test_preprocess.py            → 通过 44 项，失败 0 项

:: ④ 冒烟训练（检测 / 分类）
python scripts\train.py --task detection --source cvefixes --model models\microsoft__codebert-base ^
    --epochs 1 --batch-size 8 --max-length 64 --max-train-samples 64 --max-eval-samples 32 ^
    --log-steps 4 --run-name quickstart --cpu          → 15 秒，F1=0.0000
python scripts\train.py --task classification --source cvefixes --model models\microsoft__codebert-base ^
    --epochs 1 --batch-size 8 --max-length 64 --max-train-samples 64 --max-eval-samples 32 ^
    --log-steps 4 --run-name quickstart_cls --cpu      → 21 秒，macro-F1=0.0208

:: ⑤ 推理
python scripts\predict.py --checkpoint outputs\demo_detection\best --file demo.c --device cpu
python scripts\predict.py --checkpoint outputs\quickstart\best ^
    --input docs\test_cases\detection_test_cases.jsonl ^
    --output docs\test_cases\out_detection.jsonl --device cpu --batch-size 4     → 准确率 0.4
python scripts\predict.py --checkpoint outputs\quickstart_cls\best ^
    --input docs\test_cases\classification_test_cases.jsonl ^
    --output docs\test_cases\out_classification.jsonl --device cpu               → 7 条 Top-1 全错

:: ⑥ 教学版端到端
cd D:\workbuddy_workspace\vuln_bert_教学版\_02_bert检测
python _06_bert_predict_fun.py                       → 安全代码 / 置信度 0.6194
python _tmp_web_test.py                              → GET 200 / POST 200
```
